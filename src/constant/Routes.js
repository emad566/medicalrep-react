const DOMAIN = "https://api.wicckbot.com/";
const apiUrl = `${DOMAIN}api/`;

export default {
    appVersion : '4.1.6',
    apiUrl: apiUrl,
    ROLES: `${apiUrl}roles`,
    PERMISSIONS: `${apiUrl}permissions`,
    USERS: `${apiUrl}users`,
};

