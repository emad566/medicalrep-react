import React from "react";
import { useTranslation } from "react-i18next";

const CreateSpecialist: React.FC<any> = () => {
    const { t } = useTranslation();
    const initialValues = {

    };
    const validationObj = {

    };
}

export default CreateSpecialist;